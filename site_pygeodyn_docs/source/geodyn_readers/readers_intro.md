# Reading the GEODYN Output:


The GEODYN output reader is split up into two python scripts: `a` and `b`.

The script `a` is used to (cleanly) call the functions contained in the `b` script.  

The `b` script contains all the primary functions that interpret and read the GEODYN output. 